# RedlineSpec /health-check

Use the `health-check` skill to execute the RedlineSpec `/health-check` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@health-check` and follow it.
