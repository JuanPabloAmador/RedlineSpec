# RedlineSpec /bootstrap-functional-truth

Use the `bootstrap-functional-truth` skill to execute the RedlineSpec `/bootstrap-functional-truth` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@bootstrap-functional-truth` and follow it.
