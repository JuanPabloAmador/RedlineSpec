# RedlineSpec

Use the `redlinespec` skill for minimal RedlineSpec orientation and to choose the next workflow.

If Cascade does not automatically invoke the skill, explicitly load `@redlinespec` and follow it.
