# RedlineSpec /merge-functional-truth

Use the `merge-functional-truth` skill to execute the RedlineSpec `/merge-functional-truth` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@merge-functional-truth` and follow it.
