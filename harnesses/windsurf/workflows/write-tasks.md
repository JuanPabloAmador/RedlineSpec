# RedlineSpec /write-tasks

Use the `write-tasks` skill to execute the RedlineSpec `/write-tasks` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@write-tasks` and follow it.
