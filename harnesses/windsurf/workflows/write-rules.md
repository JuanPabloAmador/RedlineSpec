# RedlineSpec /write-rules

Use the `write-rules` skill to execute the RedlineSpec `/write-rules` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@write-rules` and follow it.
