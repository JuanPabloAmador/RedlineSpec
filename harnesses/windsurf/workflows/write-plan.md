# RedlineSpec /write-plan

Use the `write-plan` skill to execute the RedlineSpec `/write-plan` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@write-plan` and follow it.
