# RedlineSpec /close-spec

Use the `close-spec` skill to execute the RedlineSpec `/close-spec` workflow.

If Cascade does not automatically invoke the skill, explicitly load `@close-spec` and follow it.
